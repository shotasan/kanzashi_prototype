<h1>マイページ</h1>

<div class="user-info">
  <p>名前 : <%= @user.name %></p>
  <p><%= image_tag @user.icon.url %></p>
  <p>メールアドレス : <%= @user.email %></p>
  <p>プロフィール : <%= @user.profile %></p>
  <%= link_to "ユーザー編集", edit_user_registration_path, class: "btn btn-success" %>
</div>

<h2>自分の記録一覧</h2>

<div class="row fav-index">
  <% user_beans.each do |bean| %>
    <div class="col-6">
      <%= render "beans/showbean", coffee: bean %>
      <%= link_to '詳細', bean_path(bean), class: "btn btn-success"%>
    </div>
    <div class="col-6">
      <%= render "tastes/showtaste", coffee: bean %>
    </div>
  <% end %>
</div>

<h2>お気に入り一覧</h2>

<h5>ストレートコーヒー</h5>

<% @bean_favorites.each do |favorite| %>
  <div class="row fav-index">
    <div class="col-6">
      <%= render "beans/showbean", coffee: favorite %>
      <%= link_to '詳細', bean_path(favorite) %>
    </div>
    <div class="col-6">
      <%= render "tastes/showtaste", coffee: favorite %>
    </div>
  </div>
<% end %>

<h5>ブレンドコーヒー</h5>

<% @blend_favorites.each do |favorite| %>
  <div class="row fav-index">
    <div class="col-6">
      <%= render "my_blends/showblend", coffee: favorite %>
      <%= link_to '詳細', my_blend_path(favorite), class: "btn btn-success" %>
    </div>
    <div class="col-6">
      <%= render "tastes/showtaste", coffee: favorite %>
    </div>
  </div>
<% end %>